fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
# Postgres driver. Not needed for the default SQLite setup, but free hosts
# without a persistent disk require an external database, and installing it
# unconditionally means DATABASE_URL is the only thing that has to change.
psycopg[binary]>=3.1
pydantic>=2.7
pydantic-settings>=2.3
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
joblib>=1.4
requests>=2.32
python-dotenv>=1.0
pytest>=8.2
httpx>=0.27
